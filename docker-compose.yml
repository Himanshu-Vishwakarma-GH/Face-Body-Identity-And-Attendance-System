services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    container_name: ai-access-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./ml/models:/app/ml/models
    restart: unless-stopped
